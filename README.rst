Heuristic for the Container Loading Problem (CLP)
==================================================

In the Container Loading Problem, a set of parallelepipedic elements (box) must be loaded into a container; and the volume of the boxes actually loaded must be maximized.